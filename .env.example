# Configuração do Servidor
PORT=3000
NODE_ENV=development

# Cloudflare Tunnel (atualizar quando reiniciar)
# PUBLIC_URL=https://forecasts-toner-decide-previous.trycloudflare.com

# Ambiente (development = mantém arquivos, production = deleta após download)
# NODE_ENV=production
